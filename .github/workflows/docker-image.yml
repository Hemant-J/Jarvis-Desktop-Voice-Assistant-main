name: Docker Image CI

on:
  [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the python
      run: docker build -f ./Dockerfile jarvisdesktopvoiceassistantmain
